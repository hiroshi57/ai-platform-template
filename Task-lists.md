# Task-lists.md — ファクトチェック・ダッシュボード プロフェッショナル化バックログ

> 対象: `frontend/fact-check-dashboard.html`
> 目的: 「プロフェッショナルサービス」として恥ずかしくないUI品質・機能の作り込み・インフラの堅牢性を実現する。
> 凡例: `[x]` 完了(実装+検証済み)、`[ ]` 未着手。優先度 P0(最優先)〜P2(あれば良い)。
> **重要**: `[x]` は実際にコードを書いてテスト/目視確認まで終えたものだけに付ける。着手しただけ・途中のものは `[ ]` のまま残し、備考に進捗を書く。

---

## A. デザインシステム・ビジュアル刷新 (14)

- [x] A1 (P0) デザイントークン化: 色・余白・角丸・影・タイポグラフィのスケールをCSS変数に統一する
- [x] A2 (P0) 配色をプロフェッショナル向けに再設計する(現行の学習教材的な水色基調から、信頼感のあるニュートラル+アクセントカラーへ)
- [x] A3 (P0) 絵文字アイコン(🔎📋📊等)をインラインSVGアイコンセットに置き換える
- [x] A4 (P0) ボタン/入力/セレクトのフォーカスリング・ホバー・アクティブ状態を統一デザインで整備する
- [x] A5 (P1) タイポグラフィスケール(見出し/本文/補助文の階層)を明確化し行間・字間を調整する
- [x] A6 (P1) カードのシャドウ・境界線を階層に応じて使い分ける(浮き上がり感の演出) — カードはshadow-sm、トースト/フローティング保存バーはshadow-lgで差別化
- [x] A7 (P1) トップナビをピル型タブから、プロダクトらしいセグメントコントロール/サイドバー候補を検討し改善する — アイコン付きセグメントコントロール化(サイドバー化は見送り)
- [x] A8 (P1) KPIタイルにアイコン・前月比トレンド矢印を追加する — 平均スコア/誤り比率に前月比▲▼を実装、テストあり
- [ ] A9 (P2) ダークモード対応の設計だけ先に用意する(CSS変数のlight/dark切替に耐える構造) — 未着手
- [x] A10 (P1) ボタンの主従関係(primary/secondary/ghost/danger)の視覚的一貫性を見直す
- [x] A11 (P2) ロゴ/ブランドマークを絵文字から専用SVGマークに差し替える — h1にグラデーション円+検索アイコンのブランドマーク
- [ ] A13 (P2) ローディング/処理中状態(スケルトン・スピナー)を追加する — ボタンの読み込みスピナー(is-loading)は共有保存ボタンで実装したが、コンテンツのスケルトン表示は未実装
- [x] A12 (P1) 空状態(データ0件)のイラスト・ガイダンス文言を改善する — 一覧/ダッシュボードにアイコン+見出し付き空状態、テストあり
- [x] A14 (P1) 印刷レイアウトのタイポグラフィ・余白をレポートらしく整える — 新デザイントークンが印刷にも継承されることを実ブラウザのスクリーンショットで確認

## B. インフォメーション・アーキテクチャ / ナビゲーション (8)

- [x] B1 (P1) タブに未読/要対応バッジ(例: 根拠不足の件数)を表示する — 根拠ゼロで確定判定の件数を「チェック一覧」タブに表示、テストあり
- [ ] B2 (P2) パンくず/現在地表示を追加する(編集中の主張タイトルをヘッダーに表示する等)
- [x] B3 (P1) タブを行き来する際のスクロール位置・フォーカスを保持する — scrollイベントで継続的に位置を記録し、タブ切替時に復元。切替後はアクティブなタブボタンへフォーカス移動。実ブラウザ検証で「クリック時点で保存する」実装が実際は機能しないバグ(デスクトップ幅ではnavが非stickyのためクリック時には既にスクロール位置が失われている)を発見し、継続記録方式に修正
- [ ] B4 (P2) キーボードショートカット(例: `n`で新規、`/`で検索フォーカス)を実装する
- [ ] B5 (P2) 直近開いたケースへのクイックアクセス(最近使った項目)を追加する
- [ ] B6 (P2) グローバル検索(全タブ横断でケースを検索できるコマンドパレット的UI)を検討する
- [ ] B7 (P2) タブ間のディープリンク(URLハッシュでタブ状態を保持し、リロードしても同じ画面に戻れる)
- [x] B8 (P1) モバイルでのタブナビゲーションをボトムナビ化する(親指操作のしやすさ) — 実ブラウザのスクリーンショットで確認

## C. データビジュアライゼーション強化 (10)

- [x] C1 (P0) SVG折れ線グラフにグラデーション塗り・ホバーツールチップを追加する — ネイティブ`<title>`によるツールチップ、テストあり
- [x] C2 (P1) 判定内訳を横棒だけでなくドーナツチャートでも表示する — SVGドーナツ(stroke-dasharray方式、外部ライブラリ不使用)を横棒一覧と併記。色だけに頼らないF8方針を踏襲し、横棒側のテキストラベルを主たる情報源として維持
- [x] C3 (P1) 出所区分×判定のクロス集計をヒートマップで可視化する — セルの濃淡(JSでRGB補間、CSS color-mix()は互換性のため不使用)+数値を併記するテーブル形式ヒートマップ
- [x] C4 (P2) 月次トレンドに前年同月比較(データがあれば)を追加する — `computeYoyRows()`で前年同月のデータがある月だけを比較(件数・平均スコア差)、1年未満のデータしか無い場合は「データがまだありません」と正直に案内。実ブラウザ・jsdom両方で確認済み
- [x] C5 (P2) スコア分布のヒストグラムを追加する — `svgHistogram()`で総合スコアを10点刻みでバケット化しSVG棒グラフ化。実ブラウザ・jsdom両方で確認済み
- [x] C6 (P2) 軸別内訳テーブルをレーダーチャート(五角形)でも可視化する — `svgRadarChart()`。記録が無い軸は0扱いにせずnullのまま(平均計算から除外)。実ブラウザ・jsdom両方で確認済み
- [x] C7 (P2) ダッシュボードのKPIカードをドラッグで並び替え可能にする — HTML5 drag&drop、並び順は専用storageキー(`fc_dashboard_kpi_order_v1`)に保存し次回訪問時も反映。実ブラウザで実際にマウスドラッグして並び替わることを確認済み
- [x] C8 (P1) グラフの数値ラベルの重なりを防ぐ自動間引きロジックを入れる — 最大8ラベル程度に間引き、両端は必ず表示。テストあり
- [x] C9 (P2) 印刷時にグラフが正しく1ページに収まるようレイアウト調整する — 新規追加したチャート含め全SVGを`width:100%;max-width:...px;height:auto`パターンに統一し、印刷CSSでも`max-width:100%`を強制。実ブラウザの印刷メディアエミュレーションでSVG幅がカード幅に収まることを数値で確認済み
- [x] C10 (P2) CSVエクスポートに加えて簡易PNGエクスポート(グラフ単体の画像化)を追加する — `exportSvgAsPng()`(SVGをシリアライズしCanvas経由でPNG化)。Canvas未対応環境(jsdom)では例外を投げず警告するグレースフルデグラデーション。実ブラウザでダウンロードイベント発火を確認済み

## D. フォーム・入力体験 (10)

- [x] D1 (P0) 必須項目のインラインバリデーション(赤枠+エラーメッセージ)を追加する — 主張タイトル/本文の未入力時にフィールド単位のエラー表示+トースト
- [x] D2 (P1) 主張本文の文字数カウンタを表示する — 4000字上限、3600字超で警告色
- [ ] D3 (P2) 事実/意見/推測のタグ付けを、選択したテキスト範囲から自動生成できるようにする(範囲選択→タグ付けボタン)
- [ ] D4 (P2) URL欄の簡易フォーマット検証(http/https以外を警告)を入れる — 未着手
- [ ] D5 (P2) 長いフォームをステッパー(進捗インジケーター付きウィザード)形式にできるオプションを検討する
- [ ] D6 (P1) フォーム離脱時(未保存の変更がある状態でタブ遷移)に確認ダイアログを出す — 自動保存/復元バナーで代替しているため優先度を下げて保留(要再検討)
- [ ] D7 (P2) 入力欄にプレースホルダーだけでなく実例のトグル表示(サンプル入力を見る)を追加する
- [ ] D8 (P2) 選択式(select)をより視認性の高いカスタムドロップダウン(色分けバッジ付き)に置き換える
- [ ] D9 (P2) 音声・映像の主張について、タイムスタンプ(発言の時刻)を記録する項目を追加する
- [x] D10 (P1) 保存ボタンをスクロールしても追従するスティッキーフッターにする — position:fixedのフローティングバー(position:stickyは最終要素だと機能しないバグを実装中に発見・修正)。実ビューポートでのスクリーンショットで動作確認済み

## E. 通知・フィードバック / マイクロインタラクション (6)

- [x] E1 (P0) 画面下部/右上のトースト通知システムを実装し、`flashStatus`等の場当たり的な文言表示を置き換える — role="status" aria-live="polite"、テストあり
- [ ] E2 (P2) 保存成功時にボタンにチェックマークのマイクロアニメーションを付ける
- [ ] E3 (P2) 行の追加/削除にフェードイン/アウトのトランジションを付ける
- [x] E4 (P1) 危険操作(全件削除・登録解除)にはトースト内Undo(元に戻す)を用意する — 全件削除/ライセンス解除/JSONインポート置換の3箇所、テストあり
- [ ] E5 (P2) スコア変化時に数値がカウントアップするアニメーションを付ける
- [x] E6 (P1) 保存中はボタンをローディング状態にし、二重送信を防止する — サーバー共有ボタンはis-loadingスピナー、新規チェック保存はタブ遷移により二重送信不可

## F. アクセシビリティ (a11y) (8)

- [x] F1 (P0) すべてのフォーム要素にlabelのfor/id関連付けを徹底する — `field()`がinner中の`id`を自動検出し`for`を付与。行内入力・ライセンス入力にはaria-labelを付与
- [x] F2 (P0) カラーコントラスト比をWCAG AA以上に調整する(現行の薄いグレー文字を見直す) — 主要な文字色/背景の組み合わせをWCAG計算式で検証(4.5:1以上)。文字数カウンタの色が2.56:1で未達だったため`n-400`に修正
- [x] F3 (P1) キーボードのみでの全操作(タブ移動・行追加削除・保存)を可能にする — 独自clickハンドラのdiv等は使わず、すべてネイティブbutton/input/select/aで実装
- [x] F4 (P1) 動的に追加される行・トーストにARIA live regionを設定しスクリーンリーダーに通知する — `#toast-root`にrole="status" aria-live="polite"、テストあり
- [ ] F5 (P2) 印刷用と画面用で別々のalt文言を用意する(アイコンの意味をスクリーンリーダーに伝える)
- [x] F6 (P1) フォーカストラップ(同意ゲート等のモーダル的UI)を実装する — 同意ゲート内でTabキーの循環を実装、初期フォーカスも設定(テストあり)。ただし本画面はモーダルオーバーレイではなく1タブ分のコンテンツ置き換えのため、マウスでの他タブへの遷移自体は制限していない(完全なモーダル化はスコープ外、コード内コメントに明記)
- [ ] F7 (P2) 文字サイズ変更(ブラウザのズーム・OS設定)に対してレイアウトが崩れないか検証する
- [x] F8 (P2) 色だけに依存しない判定表示(色覚多様性対応、アイコン/パターンの併用)にする — 判定バッジに記号(✓/✓~/△/✕/?)を追加し、色を見なくても判別できるようにした。判定内訳の横棒グラフは元々テキストラベル併記のため対象外

## G. パフォーマンス (6)

- [ ] G1 (P2) チェック件数が多い場合(1000件超)でも一覧描画が固まらないよう仮想スクロールを検討する
- [ ] G2 (P1) `renderAll`の全体再描画コストを見直し、差分更新できる箇所を増やす
- [x] G3 (P2) localStorageの読み書きをrequestIdleCallback等で非同期化し、入力のカクつきを防ぐ — 下書き自動保存(`persistDraft`)の実書き込みを`runWhenIdle()`(requestIdleCallback、未対応環境はsetTimeoutフォールバック)まで遅延。600msデバウンスと組み合わせて二重に負荷を軽減。実ブラウザでrequestIdleCallbackが実際に使われることを確認済み
- [x] G4 (P2) SVGグラフの再描画をthrottleし、重み変更時のカクつきを防ぐ — 汎用`debounce()`を追加し、重み入力(`w-*`)のinputイベントで`persist()+renderDashResults()`を150msデバウンス。値自体(state.weights)は即時反映し取りこぼしなし。実ブラウザで連打してもエラーが出ないことを確認済み
- [x] G5 (P2) 初回ロード時間を計測し、CSS/JSのインライン化による肥大化が問題ないか確認する — `window.__bootStats`(elapsedMs/approxKB)を起動時に記録しconsole出力。実測: 約206KB、初回描画 数ms〜100ms程度(環境依存)。単一HTMLとして許容範囲内と判断(肥大化の兆候があれば今後この数値で継続監視できる)
- [x] G6 (P2) 大量データ時のCSV/JSONエクスポートをWeb Workerで行い、UIブロッキングを防ぐ — `runHeavyExport()`で件数閾値(300件)超過時のみWeb Worker化(pureFnをtoString()でWorkerに渡しロジックの重複を回避)。Worker生成失敗・タイムアウト(8秒)・未対応環境では例外を投げず同期フォールバック。実ブラウザで500件のCSVエクスポート時に実際にWorkerが使われることを確認済み

## H. データモデル・機能拡張 (14)

- [x] H1 (P0) スキーマバージョニングを導入し、旧バージョンのデータを安全にマイグレーションする — `SCHEMA_VERSION`を保存データに含め、読み込み時は常に`normalizeItem()`を通す方式で「読み込み時マイグレーション」を実現
- [x] H2 (P0) 保存データの欠損フィールド(history/archiveUrl等)を読み込み時に補完する正規化関数を実装する — `normalizeItem`/`normalizeWeights`。実装中に発見した実バグ(`AXIS_ORDER_FALLBACK`のvar初期化順序問題でloadState()がクラッシュ)も修正。テスト3件追加
- [x] H3 (P1) タグ機能(自由記述のラベル付け・タグでの絞り込み)を追加する — `tags`フィールド追加(スキーマv5)、Enter/カンマ区切りのチップ入力UI、一覧タブにタグ列(チップ表示)・タグ絞り込みドロップダウン・キーワード検索にもタグを含める。実ブラウザ・jsdom両方で確認済み
- [x] H4 (P2) 案件同士の関連付け(同じ主張の複数バリエーションをグルーピング)を追加する — `relatedIds`フィールド追加(スキーマv8)、編集画面に関連案件ピッカー・双方向リンク付与/解除(相手側itemも`persist()`)、一覧に関連チップ表示。実ブラウザ・jsdom両方で確認済み
- [x] H5 (P2) 添付ファイル(スクリーンショット画像)をBase64でローカル保存できるようにする — `attachments`フィールド追加(スキーマv8、上限30件)、既存の`downscaleImageToDataUrl`+IndexedDB(I3)保存を再利用、追加/削除(Undo)UI。実ブラウザ・jsdom両方で確認済み
- [x] H6 (P2) 音声/映像の主張について、文字起こしテキストを別欄で保持する — `transcript`フィールド追加(スキーマv8、上限20000字)、mediaTypeが音声/映像の時のみ入力欄を表示するトグル、印刷レポートにも反映。実ブラウザ・jsdom両方で確認済み
- [x] H7 (P1) 複数人での担当者アサイン(担当者名・ステータス: 未着手/確認中/完了)を追加する — `assignee`/`status`フィールド追加(スキーマv5→v6)、一覧タブに担当/状況列とステータス絞り込み、ダッシュボードに未着手件数・担当者未設定の示唆、CSV/印刷レポートにも反映。実ブラウザ・jsdom両方で確認済み
- [x] H8 (P2) 案件テンプレート(よくある確認パターンをテンプレ化して新規作成時に選べる)を追加する — `CASE_TEMPLATES`(5種)を追加、新規作成時のみテンプレピッカーを表示しmediaType/sourceCategory/tagsを一括設定(編集時は非表示)。実ブラウザ・jsdom両方で確認済み
- [x] H9 (P1) 判定の再確認期限(次回レビュー日)を設定できるようにする — `reviewDueDate`フィールドを追加(スキーマv4)、フォームに日付入力、一覧タブに「期限超過/まもなく/予定」バッジ列、ダッシュボードに示唆、CSV/印刷レポートにも反映。実ブラウザ・jsdomの両方で検証済み
- [x] H10 (P2) 類似案件の自動検出(claimTextの簡易類似度でリマインド)を追加する — 文字bigramベースのJaccard類似度(`charBigrams`/`jaccardSimilarity`/`findSimilarItems`)でclaimText入力中にヒント表示。**単語区切り正規表現では日本語(スペース無し文章)で機能しないバグを発見し、文字bigram方式に修正**(実ブラウザで実データ検証済み)
- [x] H11 (P1) 一括インポート時のバリデーション強化(不正な行をスキップしてレポートする) — 不正行数を表示、既存データへの追加/置換(Undo付き)を選択可能に。テストあり
- [x] H12 (P2) 案件ごとのコメント欄(複数人でのディスカッションログ)を追加する — `comments`フィールド追加(スキーマv8、上限200件)、投稿者名+本文の追加/削除(Undo)、空文字投稿は警告してブロック。実ブラウザ・jsdom両方で確認済み
- [x] H13 (P2) 通知設定(レビュー期限が近い案件をハイライト)を追加する — 一覧タブで期限超過(`reviewDueStatus==='overdue'`)の行を背景色でハイライト表示。実ブラウザ・jsdom両方で確認済み
- [x] H14 (P2) エクスポート形式にMarkdownレポート出力を追加する — `exportMarkdown()`/`mdEscape()`を追加、表・見出し形式で案件一覧をMarkdown化しダウンロード。実ブラウザ(ダウンロードイベント発火)・jsdom両方で確認済み

## I. インフラ・信頼性(堅牢性) (10)

- [x] I1 (P0) `try/catch`の粒度を見直し、想定外エラー発生時にアプリ全体が固まらないグローバルエラーハンドラを追加する — `window.onerror`/`unhandledrejection`をトースト通知に接続、3秒レート制限付き。テストあり
- [x] I2 (P0) localStorage操作を一元化したラッパーを作り、容量超過以外の例外(プライベートブラウジング等)にも対応する — `storageGetJSON`/`storageSetJSON`/`storageRemove`を実装し、下書き/ライセンス/サーバー共有情報の保存箇所を置き換え。メインデータ(`persist`/`loadState`)は正規化ロジックが複雑なため個別実装のまま(理由をコード内コメントに明記)。テストあり
- [x] I3 (P2) IndexedDBへの移行を検討する(localStorageの5-10MB上限を超える運用に備える) — 全面移行ではなくハイブリッド方式を採用: localStorageにはマイクロサムネイル(数KB)のみ保存し、フル解像度画像(既存の480px相当)はIndexedDBに保存して表示時に非同期取得・差し替え(プログレッシブ表示)。ハッシュ照合ロジックは同期のまま変更なし。IndexedDB非対応環境では自動的にマイクロサムネイルのみで動作継続(グレースフルデグラデーション)。実ブラウザで実際の非同期アップグレード(1463B→3263B)を確認済み。**実装過程で#app要素の委譲イベントリスナー重複という重大な既存バグを発見・修正**(詳細はCHANGELOG.md参照)
- [x] I4 (P1) 破損したJSONデータの読み込み時にアプリがクラッシュしないようフォールバックを入れる — 不正なJSON構文・不正な要素混入の両方をテストで確認
- [x] I5 (P2) 定期的な自動バックアップ(直近N世代のスナップショットをlocalStorage内に保持)を実装する — `persist()`のたびに(30分間隔でスロットリングしつつ)最大5世代のスナップショットを別storageキーに保持。「チェック一覧」タブに専用カードを追加し、任意の時点への復元(Undo付き)が可能。実ブラウザ・jsdom両方で確認済み
- [x] I6 (P1) インポート時に既存データとのマージ/上書きを選択できるようにする(現状は全置換のみ) — 「既存に追加」「すべて置き換え(Undo可)」を選べるバナーUIを実装。テストあり
- [x] I7 (P2) オフライン時のService Worker導入を検討する(現状は単一HTMLなので優先度低) — 検討の結果、意図的に見送ることにした。理由: (1) Service Workerは`file://`プロトコルでは登録できず、本ツールの主な配布形態(単一HTMLをダブルクリックで開く)と相容れない、(2) 本ツールは初回ロード後に追加のネットワークリクエストを行わない設計のため、Service Workerが解決する「オフラインキャッシュ」の課題がそもそも発生しない。判断根拠を`FACT-CHECK-DASHBOARD.md`に明記した
- [x] I8 (P2) バージョン情報(このHTMLファイル自体のビルド日時/バージョン番号)を画面に表示する — `APP_VERSION`/`APP_RELEASED_AT`をフッターに表示。「常にアップできるループ」の一環としてCHANGELOG.mdと連動
- [x] I9 (P2) サーバー共有機能の競合検知(複数人が同時編集した場合の警告)を追加する — 保存(PUT)直前に最新のupdatedAtを取得し、自分が最後に把握していた時点と食い違っていたら警告ダイアログを出す楽観的ロック方式(`checkShareConflictThenSave`)。サーバー側にロック機構が無いため完全な排他制御ではないことをダイアログ文言でも明記。jsdomでモックfetchによる競合あり/なし/初回保存の3パターンを検証済み
- [x] I10 (P0) 本番想定のエッジケース(空文字・極端に長い文字列・特殊文字)に対する耐性をテストで担保する — 破損JSON、不正な配列要素、CSVの特殊文字エスケープ等をテストでカバー

## J. テスト・CI/CD (8)

- [x] J1 (P0) `fact-check-dashboard.test.cjs`をGitHub Actions等のCIで自動実行する設定を追加する — `.github/workflows/ci.yml`のfrontendジョブに`npm run test:fact-check-dashboard`ステップを追加
- [x] J2 (P1) 新規UI要素(トースト・バリデーション・スキーマ移行)のテストケースを追加する — 18件→32件に拡充(トースト/Undo/バッジ/正規化/インポート/エラーハンドラ/チャート/KPIトレンド等)
- [x] J3 (P2) PlaywrightをdevDependency化し、ビジュアルリグレッションテスト(スクリーンショット比較)をCI化する — `@playwright/test`導入、`visual-tests/dashboard.visual.spec.js`で主要3画面をtoHaveScreenshot()比較。**正直な備考**: OS間のフォントレンダリング差でCIでの自動比較は誤検知しやすいと判断し、あえてCI化せず「ローカルで生成・比較する手動ツール」として位置づけた(実際にベースライン生成→無変更で比較PASS→意図的に大きな見た目変更を入れて比較FAIL、の3パターンを実地確認しリグレッション検知が機能することを確認済み)
- [x] J4 (P2) Lighthouse CIでパフォーマンス/アクセシビリティスコアを継続計測する — `scripts/lighthouse-check.mjs`(簡易静的サーバー+Lighthouse実行、しきい値判定で非ゼロ終了)。実行して発見した実際のアクセシビリティ不備4件(ボタンのアクセシブルネーム欠如・コントラスト比不足・タップ領域不足・main landmark欠如)を修正し、スコアを84→100に改善した
- [x] J5 (P2) HTMLバリデータ(W3C Validator相当)をCIに組み込む — `html-validate`導入、意図的にID重複を混入させて実際に検知することを確認済み
- [x] J6 (P2) テストカバレッジ(主要関数の網羅率)を計測し、抜け漏れの多い関数を洗い出す — c8(V8カバレッジ)はjsdomのvm実行構造上ファイルパスが一致せず0%表示になったため不採用とし、トップレベル関数の呼び出し回数を計測する独自ツールを実装。実測89%→未テストだった④AI参照/⑤専用ツール行描画・ガイドタブにテストを追加し92%に改善
- [x] J7 (P2) 複数ブラウザ(Firefox/Safari相当のWebKit)でのクロスブラウザテストを追加する — `visual-tests/dashboard.spec.js`(決定的なスモークテスト、CIにも組み込み)。Chromium/Firefox/WebKitの3エンジンで主要フローが実際にエラー無く動作することを確認済み
- [x] J8 (P2) リリース前チェックリスト(手動QA項目)をドキュメント化する — `frontend/RELEASE-CHECKLIST.md`を新規作成

## K. セキュリティ (6)

- [x] K1 (P0) すべてのユーザー入力のHTMLエスケープ漏れがないか再監査する(新規追加したトースト/バリデーション文言含む) — トーストはtextContent、フィールドエラーはDOM生成で構築しHTML注入経路なし。it.claimTitle等のHTML描画箇所は全てesc()経由であることをgrepで確認
- [x] K2 (P1) インポートしたJSONに巨大なペイロード・深いネストがあった場合のDoS的挙動を防ぐ上限を設ける — ファイルサイズ上限(チェックDB20MB/参照メディアDB50MB)、件数上限(5000件/2000件)、normalizeItem内で文字列長・配列長を上限で切り詰め(claimText 20000字、note系5000字、配列500件等)
- [x] K3 (P1) 外部リンク(target="_blank")すべてに`rel="noopener noreferrer"`が付与されているか確認する — 全6箇所を`noopener`のみから`noopener noreferrer`に統一
- [x] K4 (P2) Content-Security-Policyのmetaタグを追加し、想定外スクリプト実行を軽減する — `default-src 'none'`を基本に、単一HTML(インラインscript/style)に必要な`'unsafe-inline'`、サーバー共有APIドメインのみの`connect-src`、Web Worker用の`worker-src blob:`等を個別許可。実ブラウザでCSP違反ゼロ・全機能(ライセンス検証/CSV・PNGダウンロード)が正常動作することを確認済み。**追記(N11実装時に発覚した回帰)**: `media-src`を設定していなかったため`<video>`要素のdata:動画読み込みがCSP違反でブロックされていた。実ブラウザ検証で発見し`media-src data: blob:`を追加して解消(jsdomでは検出できず、実際に動画アップロード機能を実ブラウザで動かして初めて見つかったバグ)
- [x] K5 (P2) ライセンスキーの検証ロジックをより解析されにくい形に難読化する(根本解決ではないが抑止力を上げる) — `LIC_SECRET`の平文文字列リテラルを廃止し、XOR難読化したバイト配列から実行時に復元する`_licSecret()`に変更。根本的な防止策ではない旨をコード内コメントに明記。ソース内に平文の秘密値が存在しないことをテストで確認
- [x] K6 (P1) サーバー共有機能で送信するデータに機密情報(APIキー等)が誤って混入しないかチェックする仕組みを入れる — OpenAI/AWS/Google風のAPIキー・Bearerトークン・秘密鍵ヘッダのパターンを送信前にスキャンし、該当時は確認ダイアログを出す(完全な検知ではない旨は実装のみで、UI上には未記載)

## L. ビジネス・法務 (6) — 前回セッションからの持ち越し

- [ ] L1 (P0) 提供者名(PROVIDER_NAME)が正しい主体か、ユーザー本人に最終確認してもらう
- [ ] L2 (P0) 価格ページ(LEGAL_PRICING_URL)を本ツール専用のものに差し替える、または「準備中」表示にする
- [ ] L3 (P1) ライセンス方式の限界(オフライン検証はバイパス可能)について、契約書側での抑止をどう担保するか方針を決める
- [ ] L4 (P1) 本ツール固有の利用条件7項目について、実際に弁護士レビューを受ける
- [ ] L5 (P2) 商標・ロゴの正式なブランドガイドラインを整備する
- [ ] L6 (P2) 導入事例・利用規約の多言語化(英語版)を検討する

## M. ドキュメント・オンボーディング (6)

- [ ] M1 (P2) 初回起動時のオンボーディングツアー(主要機能を軽く紹介するステップ)を追加する
- [x] M2 (P1) `frontend/fact-check-dashboard.html`専用のREADMEセクション(使い方・テスト実行方法)を追加する — `frontend/FACT-CHECK-DASHBOARD.md`を新規作成
- [ ] M3 (P2) よくある質問(FAQ)をガイドタブに追加する
- [ ] M4 (P2) サンプルデータ(デモ用ダミーケース)を読み込むボタンを追加し、初見でも触って理解できるようにする
- [x] M5 (P2) 変更履歴(CHANGELOG)をツール内または別ファイルで管理する — `frontend/CHANGELOG.md`を新規作成。「常にアップできるループ」の運用ルール(実装→テスト→バージョン更新→記録→再配布)も明文化
- [ ] M6 (P2) 動画マニュアル・スクリーンキャストへのリンクを用意する

## N. 誤情報スクリーニング機能(技術×人間のトリアージ) (12)

> ユーザー依頼(2026-08-26)に基づき新規追加。「技術は絞り込みの道具、最終判断は人」という
> Human-in-the-Loopの原則のもとに設計した、既存①〜⑥(詳細検証)の**前段**にあたる
> 一次スクリーニング機能。

- [x] N1 (P0) A) パターン検知: 過去の誤情報の「型」(権威借用・緊急煽り・匿名情報源・断定表現・隠蔽主張・時期曖昧・金銭誘導・感情過多)を正規表現ベースで検知し「怪しさスコア」を算出する — `MISINFO_PATTERNS`/`scanPatterns()`、claimText入力に応じてライブ更新。⚠ 機械学習ではなく固定パターン集の静的照合(コード内コメント・ガイドタブに明記)
- [x] N2 (P0) B) 投稿者アカウントの信頼性チェック: 記録者が目視確認した項目(アカウント作成の新しさ・プロフィール画像の有無・フォロー比率・投稿履歴・所属の裏付け・認証バッジ・なりすまし疑い等)をチェックボックスでスコア化する — 外部SNS APIには接続しない(手動観察の定量化のみ)
- [x] N3 (P0) C) 過去メディアとの照合: 画像の知覚ハッシュ(dHash、Canvas APIのみ・外部送信なし)を計算し、チーム自身が育てる参照メディアDB(ローカルStorage)と比較して使い回しの可能性を検出する — 実ブラウザ(Chromium)で「オリジナル画像」→登録→「わずかに変えた画像」→距離2/64で一致検出、「全く違う画像」→不一致、を実地確認済み
- [x] N4 (P1) 参照メディアDB管理用の専用タブ(一覧・エクスポート/インポート・削除+Undo)を追加する
- [x] N5 (P1) 生成AI・合成の疑いに関する目視チェックリスト(手・指の歪み、影・反射の矛盾、瞬きの不自然さ、メタデータ不整合、模様の破綻)を追加する — ⚠ 自動検出ではなく人間の確認補助
- [x] N6 (P1) 上記A/B/Cのスコアを既存の①〜⑤の重み付き検証スコア(computeOverall)には混ぜず、明確に別物として提示する — 「スクリーニング(参考)」と「詳細検証(根拠に基づく)」を画面上でも見出しレベルで分離
- [x] N7 (P1) CSVエクスポート・印刷レポートに怪しさスコア/アカウントリスク/過去メディア一致件数を追加する
- [x] N8 (P1) ダッシュボードに一次スクリーニング由来の示唆(高リスク案件数・過去メディア一致件数)を追加する
- [x] N9 (P1) 旧スキーマ(v2以前)のデータ・下書きバックアップを安全に読み込めるようマイグレーションする(SCHEMA_VERSION 2→3) — 実装中に発見した実バグ(クラッシュ復旧時のnormalizeItem漏れ)も修正
- [x] N10 (P0) 新機能のテストを追加する(パターン検知/アカウントスコア/ハミング距離/参照DB照合の純粋ロジック + UI連動 + Canvas未対応環境での安全性)。Canvasに依存する実際の画像ハッシュ照合はjsdomでは検証できないため、Playwrightの実ブラウザで別途end-to-end検証した
- [x] N11 (P2) 動画・音声ファイルの自動照合(現状は画像のみ対応) — 動画は代表フレーム(冒頭1秒付近)を1枚抽出し、既存の画像知覚ハッシュパイプラインに乗せる方式で対応(スキーマv9、`mediaCheck.sourceType`追加)。**正直な限界**: 動画全体・音声までは照合しない(該当フレームが誤情報の核心でない場合や別場面が使い回されている場合は検出できない)ことをUI・印刷レポート両方に明記。音声ファイル単体の自動照合は依然未対応。ffmpegで生成したテスト動画を実際にアップロード→フレーム抽出→事前登録した参照画像との一致検出、を実ブラウザで確認済み(過程でCSPのmedia-src欠如というK4の回帰バグも発見・修正)
- [x] N12 (P2) パターンカタログ(MISINFO_PATTERNS)を画面上のUIから編集・追加できるようにする(現状はコード直接編集のみ) — 「ガイド・参考ツール」タブにカスタムパターン管理UIを追加(追加/削除+Undo)。安全のため正規表現ではなくキーワード部分一致方式に限定(ユーザー入力を`new RegExp()`に渡すReDoS等のリスクを避けるため)。実ブラウザ・jsdom両方で確認済み

---

## 進行ログ

このセクションは実装が進むたびに追記する(いつ・何を・どう検証したか)。

### 2026-08-25: 初回の作り込みセッション

「プロフェッショナルサービスを目指す」という方針のもと、100件のバックログを作成した上で
P0中心に26件を実装・検証した(内訳は各タスクの `[x]` と備考を参照)。

**実装したもの(要約)**:
- デザインシステム刷新(トークン化・配色・SVGアイコン化・ボタン/フォーム/カードの統一)
- トースト通知システム(Undo付き)でflashStatus等の場当たり的な通知を置き換え
- インラインバリデーション・文字数カウンタ・フローティング保存バー
- スキーマバージョニング+正規化関数(旧データの欠損フィールド補完)
- グローバルエラーハンドラ、localStorage一元化ラッパー
- JSONインポートのマージ/置換選択(Undo付き)、不正行のスキップ+レポート
- 折れ線グラフのツールチップ・グラデーション・ラベル間引き
- KPIの前月比トレンド表示
- 基本的なアクセシビリティ対応(label関連付け・コントラスト比・ARIA live・フォーカストラップ)
- セキュリティ再監査(エスケープ・rel="noopener noreferrer")
- CIへのテスト組み込み、専用ドキュメント(`frontend/FACT-CHECK-DASHBOARD.md`)追加
- テストを18件→32件に拡充

**実装中に発見・修正した実バグ(自己レビューで検出、jsdomでは検出できず実ブラウザで発覚したものも含む)**:
1. `normalizeWeights`が参照する`AXIS_ORDER_FALLBACK`のvar初期化順序バグ(起動直後のloadState()が旧データ読み込み時にクラッシュしていた)
2. 事実/意見/推測の記述行(segments)でCSS Gridの列数と子要素数が食い違い、削除ボタンが横幅いっぱいのピンクの帯になっていた
3. `position:sticky`の保存ボタンバーが「最後の要素」だと機能しない(スクロールの余地が無いため)ことが判明し、`position:fixed`のフローティングバーに変更。フルページスクリーンショットでは`position:fixed`要素が正しく合成されない既知のPlaywright/Chromiumの制約があるため、実際のビューポートでスクロールしながらのスクリーンショットで動作を再確認した
4. 文字数カウンタの色(`--n-300`)がWCAG AA基準(4.5:1)を満たさず2.56:1だったため`--n-400`に修正

**あえて実装しなかったもの(理由付き)**:
- D6(フォーム離脱時の確認ダイアログ): 自動保存+復元バナーで実質的なデータ損失リスクは既にカバーされているため、追加の確認ダイアログはUX上のノイズになりうると判断し保留
- A13のスケルトン表示: ボタン単位のローディングスピナーは実装したが、コンテンツ全体のスケルトンは同期処理のみのため必要性が薄く未実装
- F6の完全なモーダル化: 同意ゲートはモーダルオーバーレイではなく1タブ分のコンテンツ置き換えという設計のため、Tabキーの循環と初期フォーカスのみ実装し、マウスでの他タブ遷移の制限は行っていない

**未着手の主な項目(次回優先)**: B3(スクロール位置保持)、F8(色以外での判定表示)、K2(インポートのDoS対策)、
L1〜L6(ビジネス・法務、前回セッションからの持ち越し)。

### 2026-08-26: 一次スクリーニング機能(N1〜N10)の追加

ユーザーから提示された3つの手法(①誤情報パターンのスコアリング、②アカウント信頼性チェック、
③過去メディアデータベース照合)と、参考として共有された「技術と人を組み合わせる」スライドの
考え方(Spectee社の実例と推測される内容を参考に、一般的なAI活用原則として自分の言葉で再構成)を
踏まえ、既存の①〜⑥(詳細検証)の前段となる一次スクリーニング機能(A/B/C)を実装した。

**実装したもの**: 上記N1〜N10(詳細はNセクション参照)。特に③の画像照合は、Playwrightの実ブラウザで
「オリジナル画像を登録→わずかに変更した画像をアップロード→距離2/64で正しく一致検出」
「全く異なる画像をアップロード→正しく不一致」という実地テストで、知覚ハッシュのロジックが
実際に機能することを確認した(jsdomはCanvas非対応のため、この検証は実ブラウザでしかできない)。

**正直に明記したこと**:
- パターン検知は「過去のデマを学習する」という表現をそのまま使わず、機械学習ではなく固定パターン集の
  静的照合であることをコード内コメント・ガイドタブの両方に明記した
- アカウント信頼性チェックはSNS事業者のAPIに自動接続するものではなく、記録者の目視確認を
  定量化するものであることを明記した
- 過去メディア照合は世界規模のデータベースではなく、このチームが自ら育てるローカルDBであることを
  繰り返し明記した(件数が少ないうちは「一致なし」が「本物」を意味しないことも明記)
- 生成AI/合成の検出は自動判定ではなく、人間向けの目視チェックリストとして実装した
- これら一次スクリーニングのスコアは、既存の①〜⑤に基づく検証スコア(computeOverall)には
  混ぜていない。トリアージ用の別スコアとして明確に分離し、画面上でも見出しレベルで区別した

**実装中に発見・修正した実バグ**: クラッシュ復旧(下書き自動保存からの復元)時に`normalizeItem()`を
通していなかったため、旧スキーマの下書きを復元すると新フィールド欠損でクラッシュしうる状態だった。
今回のテスト作成中に発見し、`state.draft = normalizeItem(backup.draft)` に修正した。

**次回以降の課題**: N11(動画・音声の自動照合は未対応)、N12(パターンカタログのノーコード編集)。

### 2026-08-27: B3/F8/K2/K6を実装

「OK 進めて」の指示を受け、優先度P1の残タスクから4件を実装した。

- **B3**(スクロール位置・フォーカス保持): 実装当初は「タブ切替クリックの瞬間にscrollYを保存する」
  方式だったが、Playwrightの実ブラウザテストで**機能していないことを発見**した。原因は、
  デスクトップ幅ではタブナビゲーションが画面上部に固定(sticky/fixed)されておらず、
  ページを下にスクロールした状態からタブをクリックするには先にナビゲーションが見える
  位置まで戻る必要があり、クリックのハンドラが実行される時点では既にスクロール位置が
  失われていたため。`scroll`イベントで継続的に位置を記録する方式に変更して解決した。
  jsdomの自動テストだけでは検出できず、実ブラウザでの目視+数値検証で初めて見つかった。
- **F8**(色以外での判定表示): 判定バッジに記号(✓/✓~/△/✕/?)を追加。
- **K2**(インポートのDoS対策): ファイルサイズ・件数・文字列長・配列長の上限を追加。
- **K6**(機密情報の誤送信チェック): サーバー共有前に代表的なAPIキーパターンをスキャンし、
  検知時は確認ダイアログを出す簡易チェックを追加。

テストは45件→51件に拡充。すべてPASSを確認済み。

### 2026-08-27(続き): H9(再確認期限)を実装

「ひとつずつ進めて」の指示を受け、以降は1タスクずつ実装・検証・コミットする方針に切り替えた。

- **H9**: `reviewDueDate`(次回レビュー予定日、任意の日付)をスキーマに追加(v3→v4)。
  `reviewDueStatus()`で「なし/期限超過/7日以内/まだ先」を判定し、一覧タブに専用列
  (色分けバッジ)、ダッシュボードに示唆、CSV・印刷レポートにも反映した。
  jsdomテスト5件を追加(56件→全PASS)に加え、実ブラウザで一覧のバッジ表示・
  ダッシュボードの示唆文言・印刷レポートへの反映を目視確認した。

### 2026-08-27(続き): H3→H7→C2/C3→I3の順で実装、重大バグを発見・修正

ユーザー指示「H3,H7、C2/C3、I3の順で」に沿って4項目を順に実装した(スキーマv4→v7)。

- **H3**(タグ)、**H7**(担当者・ステータス)、**C2/C3**(ドーナツチャート・ヒートマップ)は
  いずれも既存パターンの延長で実装でき、実ブラウザ確認も含めて問題なく完了。
- **I3**(IndexedDB)は全面移行ではなく、マイクロサムネイル(localStorage)+
  フル解像度画像(IndexedDB、非同期プログレッシブ表示)のハイブリッド方式を採用。
  ハッシュ照合ロジックは変更していない。
- **I3の実装・テスト作成中に、既存の重大なバグを発見した**: 「新規/編集」タブに
  2回目以降訪れると、`#app`要素が使い回される(innerHTML更新のみで再生成されない)
  ため`bindNewCheckEvents()`内の委譲イベントリスナーが積み重なり、1回のクリックで
  同じ処理が複数回実行されていた(トグルボタンが開いた瞬間に閉じる等)。
  このバグは初回訪問の動作確認だけでは発見できず、「保存→タブに戻る→もう一度操作する」
  という現実的な使い方をして初めて顕在化した。要素を複製して差し替える方式で修正し、
  再発防止の回帰テストも追加した(jsdom + 実ブラウザ両方で確認)。
- ついでに`state.draft`がnullのまま'new'タブが描画されようとするケースへの防御も追加した。
- 「常にアップできるループ」の土台として、`APP_VERSION`のフッター表示(I8)と
  `frontend/CHANGELOG.md`(M5)を追加し、実装→テスト→バージョン更新→記録という
  運用サイクルを明文化した。

テストは67件→72件に拡充。すべて実ブラウザ(Playwright)でも該当機能を確認済み。

### 2026-08-27(続き2): H4/H5/H6/H8/H10/H12/H13/H14を実装(スキーマv8)

ユーザー指示「H4/H5/H6/H8/H10/H12/H13/H14から」に沿って8項目をまとめて実装した。
スキーマをv7→v8に上げ、`transcript`/`relatedIds`/`attachments`/`comments`を追加。

- **H8**(案件テンプレート)・**H6**(文字起こし)は新規作成フォームの条件付き表示として
  素直に実装できた。
- **H4**(関連案件)・**H5**(添付ファイル)・**H12**(コメント)はいずれも配列フィールド+
  追加/削除UIというパターンで実装。H5はH3(IndexedDB画像保存)の関数をそのまま再利用した。
  H4は双方向リンクのため、リンクする相手側の既存案件を直接`state.items`から探して
  `persist()`する設計にした(現在編集中の案件はいつも通り保存時にのみ永続化)。
- **H13**(期限超過ハイライト)は既存の`reviewDueStatus()`を使い、一覧行に背景色を
  付けるだけの小さな変更。
- **H14**(Markdownエクスポート)は既存のCSVエクスポートと並ぶ形で`exportMarkdown()`を追加。
- **H10(類似案件自動検出)で日本語特有のバグを発見・修正**: 当初`jaccardSimilarity()`は
  単語境界の正規表現(`/[\p{L}\p{N}]{2,}/gu`)でトークン化していたが、日本語はスペース区切り
  がないため文全体が1つの「単語」とみなされ、実際のクレーム文では類似度が常に0近くになり
  ヒントが出ないバグがあった。実データに近い日本語文でjsdomテストを書いて発覚し、文字bigram
  (2文字ずつのシングル)方式に変更して解決。実ブラウザでも実際の日本語文で動作確認した。

テストは72件→84件に拡充。実ブラウザ(Playwright, `test-h-batch2.js`)でテンプレート適用・
文字起こし欄トグル・コメント表示・類似案件ヒント・双方向関連案件リンク・期限超過行の
ハイライト・Markdownダウンロードの全てをゼロエラーで確認済み。

### 2026-08-27(続き3): C4/C5/C6/C7/C9/C10を実装(データビジュアライゼーション強化)

ユーザーからの大きな追加指示(C4〜C10の残り、G3〜G6、I5/I7/I9、J3〜J8、K4/K5、
L1〜L6、M1/M3/M4/M6、N11/N12)のうち、まずC区分(ダッシュボードのグラフ強化)から着手した。

- **C4**(前年同月比較): `computeYoyRows()`を追加。前年の同じ月に実データが無い月は
  比較対象から除外し(0%減と誤解させないため)、1年分のデータが無い場合は
  「データがまだありません」と正直に案内する。
- **C5**(スコア分布ヒストグラム)・**C6**(軸別平均のレーダーチャート)は、
  既存のSVGチャート関数群(svgDonutChart等)と同じ設計方針(viewBox+width:100%で
  レスポンシブ、<title>でホバー情報)を踏襲して実装。C6は「記録の無い軸を0点として
  平均に含めない」既存方針(axisScores)を維持した。
- **C7**(KPIカードのドラッグ並び替え): HTML5のdrag&dropイベントで実装し、
  並び順は専用のlocalStorageキー(メインデータとは別、スキーマ移行の対象外)に保存。
  実ブラウザで実際にマウスドラッグ操作をシミュレートし、並び替え後にlocalStorageへ
  正しく保存されることを確認した。
- **C9**(印刷時のレイアウト): 新規追加分含め全チャートSVGを`width:100%;height:auto`の
  パターンに統一し、印刷用CSSでも`max-width:100%`を強制。印刷メディアエミュレーション下で
  SVGの実測幅がカード幅を超えないことを数値で確認した。
- **C10**(PNGエクスポート): SVGをシリアライズしCanvas経由でPNG化する`exportSvgAsPng()`を
  追加。IndexedDB(I3)と同じ「非対応環境では例外を投げず警告するだけ」の設計方針を踏襲し、
  jsdom(canvasパッケージ未導入)でも安全に動作することをテストで確認。実ブラウザでは
  実際にPNGダウンロードイベントが発火することを確認した。

テストは84件→97件に拡充。全機能を実ブラウザ(Playwright)でも確認済み(ゼロエラー)。
APP_VERSIONを0.9.0→0.10.0に更新。

### 2026-08-27(続き4): G3/G4/G5/G6を実装(パフォーマンス改善)

C区分に続き、G区分(パフォーマンス)の4件を実装した。

- **G3**: 下書き自動保存の実書き込みを`runWhenIdle()`(requestIdleCallback、未対応環境は
  setTimeoutにフォールバック)まで遅延。既存の600msデバウンスと合わせて二重に負荷を抑える。
- **G4**: 重み入力に汎用`debounce()`(150ms)を適用し、連打・スピナー操作時の
  チャート再描画の無駄打ちを防止。値自体(state.weights)は即時反映するため、
  最終的な入力値の取りこぼしは発生しない設計にした。
- **G5**: `window.__bootStats`に初回描画時間とファイルサイズ目安(HTML文字数から概算)を
  記録するようにした。実測値は約206KB・描画数〜数百ms(環境依存)で、単一HTMLとしては
  現時点で許容範囲内と判断した。今後の肥大化を継続監視するための仕組みとして位置づける。
- **G6**: CSV/JSONエクスポートの文字列組み立てロジックを、300件超のデータに限り
  Web Workerへオフロードするようにした。Worker側にもロジックを重複実装しないよう、
  純粋関数(`buildCsvString`/`buildExportJsonString`)を`toString()`でそのままWorker内へ
  渡す方式を採用。Worker生成失敗・8秒タイムアウト・未対応環境では自動的に同期処理へ
  フォールバックする。

テストは97件→105件に拡充。Web Workerの実使用・requestIdleCallbackの実使用・重み入力の
連打時の無エラー確認は、いずれも実ブラウザ(Playwright)で検証した(jsdomはWeb Worker/
requestIdleCallbackを実装していないため、この2点は実ブラウザでしか検証できない)。
APP_VERSIONを0.11.0に更新。

### 2026-08-27(続き5): I5/I7/I9を実装(信頼性・堅牢性)

- **I5**(自動バックアップ): `persist()`実行のたびに(30分間隔でスロットリング)最大5世代の
  スナップショットを専用storageキーへ保存。「チェック一覧」タブに一覧・復元UIを追加し、
  復元操作にはE4(危険操作のUndo)と同じ「元に戻す」トーストを実装した。
- **I7**(Service Worker): 導入を検討した結果、意図的に見送ることにした。単一HTMLファイルを
  `file://`で直接開く配布形態とService Workerのセキュアコンテキスト要件が相容れないこと、
  本ツールが初回ロード後に追加のネットワークリクエストを行わない設計であることから、
  実装しても解決する課題が無いと判断した。判断根拠は`FACT-CHECK-DASHBOARD.md`に明記した
  (「未着手のまま放置」ではなく「検討の上での不実装」であることを明確にするため)。
- **I9**(サーバー共有の競合検知): di-tools-api workspaceにサーバー側のロック機構が無いため、
  「保存直前に最新のupdatedAtを取得し、自分が最後に把握していた時点と食い違っていたら
  警告する」楽観的ロック方式で実装した。完全な排他制御ではないことをダイアログ文言にも明記。

テストは105件→114件に拡充(既存のサーバー共有テスト1件もI9の仕様変更に合わせて更新)。
自動バックアップの復元操作は実ブラウザでも目視確認済み。APP_VERSIONを0.12.0に更新。

### 2026-08-27(続き6): K4/K5を実装(セキュリティ強化)

- **K4**(CSP): `default-src 'none'`を基本方針としつつ、単一HTML(インラインscript/style)に
  必要な`'unsafe-inline'`、サーバー共有APIドメイン(`di-tools-api.vercel.app`)のみの
  `connect-src`、G6のWeb Worker(Blob URL)に必要な`worker-src blob:`を個別に許可した。
  meta要素のCSPは`frame-ancestors`等を強制できない仕様上の制約があるため含めていない
  (コード内コメントに明記)。実ブラウザでCSP違反ゼロ、かつCSV/PNGダウンロード・
  ライセンス検証が全て正常動作することを確認した。
- **K5**(ライセンス秘密値の難読化): `LIC_SECRET`という平文の文字列リテラルを廃止し、
  XOR難読化したバイト配列から実行時に復元する`_licSecret()`に変更した。根本的な
  セキュリティ対策ではない(実行時にはメモリ上に復元されるため)ことをコメントで明記。

テストは114件→117件に拡充。CSPの実ブラウザ動作確認(Worker/Canvas/fetchが阻害されないか)
はjsdomでは検証できないためPlaywrightで別途実施した。APP_VERSIONを0.13.0に更新。

### 2026-08-27(続き7): J3〜J8(テスト・CI基盤)とN11/N12(動画照合・パターンカタログ編集)を実装

- **J3/J7**(Playwright導入): `@playwright/test`を導入し、Chromium/Firefox/WebKitの
  3エンジンで決定的なスモークテスト(`visual-tests/dashboard.spec.js`)をCIに追加した。
  ビジュアルリグレッション(スクリーンショット比較、`dashboard.visual.spec.js`)は、
  ベースライン生成環境(Windows)とCI実行環境(Ubuntu)のフォントレンダリング差により
  誤検知しやすいことを実測で確認したため、あえてCI化せず「ローカルで実行する手動ツール」
  として位置づけた。実際にベースライン生成→無変更で比較PASS→意図的な見た目変更で比較FAIL、
  という3段階を自分で試し、リグレッション検知が本当に機能することを確認した。
- **J4**(Lighthouse): `scripts/lighthouse-check.mjs`を追加し、実行してみたところ
  アクセシビリティスコアが84点だった。実際の指摘内容(ボタンのアクセシブルネーム欠如・
  コントラスト比不足・タップ領域不足・main landmark欠如)を1つずつ修正し、100点まで改善した。
  特に「モバイル幅では.tab-labelがdisplay:noneになりボタンが無名になる」という、
  これまでのテストでは発見できていなかった実バグを見つけられたのは大きな収穫だった。
- **J5**(HTMLバリデータ): html-validateを導入。意図的にID重複を混入させたコピーで
  検知することを確認してから、正規のファイルが0件であることを確認した。
- **J6**(関数カバレッジ): c8(V8カバレッジ)はjsdomのvm実行構造上ファイルパスが
  一致せず0%と誤表示されることが分かったため不採用とし、トップレベル関数の呼び出し
  回数を数える独自の簡易ツールを実装した。実測89%で、④AI参照・⑤専用ツール行の描画と
  ガイドタブが一度もテストされていないことが判明し、テストを追加して92%まで改善した。
- **J8**: `frontend/RELEASE-CHECKLIST.md`を新規作成。
- **N11**(動画の自動照合): 動画全体の真のフィンガープリンティングは本ツールの規模では
  実装しない判断とし、代わりに「動画から代表フレーム(冒頭1秒付近)を1枚抽出し、
  既存の画像知覚ハッシュパイプラインにそのまま乗せる」限定的だが実際に機能する方式を
  採用した(スキーマv9)。ffmpegで生成したテスト動画を実際にアップロードし、
  事前登録した静止画フレームとの一致が正しく検出されることを実ブラウザで確認した。
  **この実装過程で、CSPに`media-src`を設定していなかったため`<video>`要素のdata:動画
  読み込みがブロックされるという実バグ(K4の回帰)を発見・修正した**。動画全体・音声は
  照合していないという限界は、UI(結果表示)・印刷レポート・ガイドタブの3箇所に明記した。
- **N12**(パターンカタログ編集): 「ガイド・参考ツール」タブにカスタムパターンの追加/削除
  UI(Undo付き)を実装。ユーザー入力をそのまま正規表現として使うとReDoS等のリスクが
  あるため、あえてキーワード部分一致方式に限定した。

テストは119件→127件に拡充。APP_VERSIONを0.14.0に更新。
