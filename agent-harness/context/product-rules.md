# 製品ルール（層2 が組み込む高信号コンテキスト）

## 日付フォーマット

- すべての日付は **ISO 8601**（`YYYY-MM-DD`）で出力する。
- 例: `2026-09-11`（OK） / `2026/09/11`（NG）

## CSV エクスポート

- ヘッダーは `date,region,revenue` の順で固定。
- 数値カラムに桁区切りや通貨記号を入れない。

> この 1 ルールが層5の `date_format` チェックの根拠になる。
> ルールが証拠に翻訳されて初めて、モデルは修復できる。
